# Anthropic API key — required for planner + agent
# Get one at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Browser headless mode — auto-set to true in CI environments.
# Uncomment to force a specific value locally.
# CI=false
